kind: Policy
spec:
  name: "{{.name}}"
  description: "{{.description | default "Read-only access policy"}}"
  statementQuery: |
    ALLOW settings:objects:read, settings:schemas:read,
          environment:roles:viewer;
